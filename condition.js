// !Advance Condition.
let x = 20 === 20 && 20 > 30;

// if X not True.
if(!x)
{
    console.log(x);

    const y = x? x:true;

    console.log(y);
}

else if(x === false)
{
    console.log("X now False");
    // here i'll not come because one condition is done.
}

